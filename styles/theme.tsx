import { extendTheme } from "@chakra-ui/react";

const customTheme = extendTheme({
    colors: {
        logo: {
            100: "#3266cf",
            900: "#000000",
        },
    },
})


export default customTheme;
